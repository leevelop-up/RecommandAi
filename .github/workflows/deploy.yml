name: Deploy to NAS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to NAS Server
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to NAS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.NAS_HOST }}
          username: ${{ secrets.NAS_USERNAME }}
          key: ${{ secrets.NAS_SSH_KEY }}
          port: ${{ secrets.NAS_PORT }}
          command_timeout: 30m
          script: |
            cd ~/docker/RecommandAi
            git pull origin main

            # 필수 디렉토리 생성
            mkdir -p output logs

            # 기존 컨테이너 완전히 제거
            /usr/local/bin/docker-compose down --remove-orphans

            # 8000 포트 사용하는 컨테이너 강제 종료
            docker ps -q --filter "publish=8000" | xargs -r docker stop

            # 빌드 및 실행
            /usr/local/bin/docker-compose build
            /usr/local/bin/docker-compose up -d

            echo "✅ RecommandAi 배포 완료"
            /usr/local/bin/docker-compose ps

      - name: Health Check
        run: |
          sleep 10
          curl -f http://${{ secrets.NAS_HOST }}:8000/health || exit 1

      - name: Notify deployment status
        if: always()
        run: |
          if [ ${{ job.status }} == 'success' ]; then
            echo "🎉 배포 성공!"
          else
            echo "❌ 배포 실패"
            exit 1
          fi
